services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aijam
      POSTGRES_PASSWORD: aijam
      POSTGRES_DB: aijam
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
